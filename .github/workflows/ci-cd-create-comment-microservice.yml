name: CI/CD for Create Comment Microservice

on:
  push:
    branches:
      - test
    paths:
      - 'create-comment-microservice/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker
        run: |
          sudo apt-get update
          sudo apt-get install -y docker.io
          sudo systemctl start docker
          sudo systemctl enable docker

      - name: Build Docker image
        run: |
          cd create-comment-microservice
          docker build -t create-comment-microservice .

      - name: Log in to AWS ECR
        run: |
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin <your-ecr-repo-url>

      - name: Tag Docker image
        run: |
          cd create-comment-microservice
          docker tag create-comment-microservice:latest <your-ecr-repo-url>/create-comment-microservice:latest

      - name: Push Docker image
        run: |
          cd create-comment-microservice
          docker push <your-ecr-repo-url>/create-comment-microservice:latest

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ec2-user@<your-ec2-ip> "
            docker pull <your-ecr-repo-url>/create-comment-microservice:latest
            docker stop create-comment-microservice || true
            docker rm create-comment-microservice || true
            docker run -d -p 7001:7001 --name create-comment-microservice <your-ecr-repo-url>/create-comment-microservice:latest
          "